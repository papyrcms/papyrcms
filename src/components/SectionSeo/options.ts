import { SectionOptions } from 'types'

export const options: SectionOptions = {
  SectionSeo: {
    component: 'SectionSeo',
    name: 'Page SEO',
    description: '',
    inputs: ['tags'],
    maxPosts: 1,
    defaultProps: {},
  },
}
